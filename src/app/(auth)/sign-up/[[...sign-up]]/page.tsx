import { SignUp } from "@clerk/nextjs";
import React from "react";
 
function Page({}) {
  return <SignUp />;
}

export default Page;


//the square brackets [] in a directory name are used to define dynamic routes.